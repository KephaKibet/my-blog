export enum ActionTypes { 
  // use enums instead-all properties have to be unique
  REGISTER = '[Auth] Register',
  REGISTER_SUCCESS = '[Auth] Register success',
  REGISTER_FAILURE= '[Auth] Register failure'

}